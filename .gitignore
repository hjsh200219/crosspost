# Never commit user state or secrets
.env
*.env
node_modules/
published*.json
queue.json
*-cache.json
browser-profile/
profile/
posts/
*.log
.DS_Store
.omc/
_workspace/

# local-only handoff backups (git history already keeps generations)
.claude-project/HANDOFF.md.prev*
